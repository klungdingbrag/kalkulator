function parseNumber(value){if(typeof value==='number')return Number.isFinite(value)?value:0;let s=String(value??'').trim().replace(/\s/g,'');if(!s)return 0;if(s.includes(',')&&s.includes('.'))s=s.replace(/\./g,'').replace(',','.');else if(s.includes(','))s=s.replace(',','.');else if(/^[-+]?\d{1,3}(\.\d{3})+$/.test(s))s=s.replace(/\./g,'');const n=Number(s);return Number.isFinite(n)?n:0}
function formatNumberValue(value){const n=parseNumber(value);return Number.isFinite(n)?new Intl.NumberFormat('id-ID',{maximumFractionDigits:6}).format(n):''}
function initNumberInputs(){document.querySelectorAll('input[type="number"]').forEach(input=>{input.type='text';input.inputMode='decimal';input.autocomplete='off';input.addEventListener('input',()=>{const start=input.selectionStart??input.value.length;const before=input.value;const digitsBefore=(before.slice(0,start).match(/[0-9]/g)||[]).length;const raw=before.replace(/[^0-9,.-]/g,'');if(raw==='-'||raw.endsWith(',')||raw.endsWith('.'))return;const formatted=formatNumberValue(raw);input.value=formatted;let pos=0,count=0;for(let i=0;i<formatted.length&&count<digitsBefore;i++){if(/\d/.test(formatted[i]))count++;pos=i+1}try{input.setSelectionRange(pos,pos)}catch(e){}})})}
function initTheme(){const dark=localStorage.getItem('theme')==='dark';document.documentElement.classList.toggle('dark',dark);const text=document.getElementById('themeText');const icon=document.getElementById('themeIcon');if(text)text.textContent=dark?'Mode Terang':'Mode Gelap';if(icon)icon.setAttribute('data-lucide',dark?'sun':'moon')}
function toggleTheme(){const dark=!document.documentElement.classList.contains('dark');document.documentElement.classList.toggle('dark',dark);localStorage.setItem('theme',dark?'dark':'light');const text=document.getElementById('themeText');const icon=document.getElementById('themeIcon');if(text)text.textContent=dark?'Mode Terang':'Mode Gelap';if(icon)icon.setAttribute('data-lucide',dark?'sun':'moon');if(window.lucide)lucide.createIcons()}
function formatRupiahGlobal(input){const digits=String(input.value||'').replace(/\D/g,'');input.value=digits?new Intl.NumberFormat('id-ID').format(Number(digits)):'';return digits?Number(digits):0}
document.addEventListener('DOMContentLoaded',initNumberInputs);